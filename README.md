## Getting Started

1. **Clone the repo and switch to dev:**
   `git checkout dev`
2. **Set up the Python Virtual Environment:**
   `python3 -m venv venv`
   `source venv/bin/activate`
3. **Install dependencies:**
   `pip install -r requirements.txt`# BioXAI: Explainable Protein Mutation Scoring
